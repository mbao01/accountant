export enum Route {
  ROOT = "/",
  LOGIN = "/login",
  LOGOUT = "/logout",
  RECORDS = "/records",
  ACCOUNTS = "/accounts",
  REGISTER = "/register",
  ADD_RECORD = "/records/add",
  RECORD_TYPE = "/records/type",
  RECORD_CATEGORY = "/records/category",
}
